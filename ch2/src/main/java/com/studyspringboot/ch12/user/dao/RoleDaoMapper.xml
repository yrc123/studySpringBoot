<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyspringboot.ch12.user.dao.RoleDao">
    <select id="findRolesByUserName" resultType="com.studyspringboot.ch12.user.pojo.Role">
        select t_role.role_name as roleName,t_role.note as note
        from t_role,t_user,t_user_role
        where t_user.user_name='${userName}' and t_user.id=t_user_role.user_id and t_role.id=t_user_role.role_id;
    </select>
</mapper>
